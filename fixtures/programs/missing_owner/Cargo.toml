[package]
name = "missing_owner"
version = "0.1.0"
description = "Otter CI fixture: missing owner check (pinocchio)"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "missing_owner"

[dependencies]
pinocchio = "0.7"
pinocchio-pubkey = "0.2.2"

