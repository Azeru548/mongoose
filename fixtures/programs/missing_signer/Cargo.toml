[package]
name = "missing_signer"
version = "0.1.0"
description = "Otter CI fixture: missing signer check (native)"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "missing_signer"

[dependencies]
solana-program = "1.18.26"
