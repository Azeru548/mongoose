[package]
name = "missing_signer_secure"
version = "0.1.0"
description = "Otter CI fixture: missing signer check - secure control (pinocchio)"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "missing_signer_secure"

[dependencies]
pinocchio = "0.8"
